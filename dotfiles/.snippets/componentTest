/* @flow */
import React from 'react'
import { mount } from 'enzyme'
import {{++}} from './{{+~expand('%:p:h:t')+}}'

test('{{++}}', () => {
  const output = mount(<{{++}} />)
  const {{++}}Node = output.find('#{{+~\~1+}}')
  expect({{+~\~2+}}Node.{{++}}).toBe({{++}})
})

/* @flow */
import React from 'react'
import { mount } from 'enzyme'
import {{++}} from './{{+~expand('%:t:r:r')+}}'

test('{{++}}', () => {
  const output = mount(<{{++}} />)
  const {{++}}Node = output.find('#{{+~\~1+}}')
  expect({{+~\~2+}}Node.{{++}}).toBe({{++}})
})
